# ==============================================================================
# Telstar Modbus Web Viewer - Konfiguration (Debug-Modus)
# ==============================================================================
# Diese Datei enthält alle Umgebungsvariablen für den Web Viewer Container
# Verwendung: docker-compose -f docker-compose.web.yml --env-file .env.web up -d
# ==============================================================================
# Der Web Viewer ist ideal für Entwicklung und Debugging:
# - Kein MQTT erforderlich
# - Web-Interface für Echtzeit-Anzeige
# - REST-API für direkten Datenabruf
# ==============================================================================

# ------------------------------------------------------------------------------
# Modbus-Konfiguration (ERFORDERLICH)
# ------------------------------------------------------------------------------
# IP-Adresse oder Hostname des Telstar/Smart-Me Zählers
MODBUS_HOST=192.168.1.100

# Modbus-TCP Port (Standard: 502)
MODBUS_PORT=502

# Modbus Unit ID (Standard: 1)
MODBUS_UNIT_ID=1

# Offset für Registeradressen (z.B. -40001 bei 40001-Adressierung)
# Standard: 0 für direkte Adressierung
MODBUS_ADDRESS_OFFSET=0

# ------------------------------------------------------------------------------
# Allgemeine Einstellungen
# ------------------------------------------------------------------------------
# Abfrageintervall in Sekunden
INTERVAL=10

# Log-Level: DEBUG, INFO, WARNING, ERROR
# Für Debugging empfohlen: DEBUG
LOG_LEVEL=INFO

# ------------------------------------------------------------------------------
# Web-Interface
# ------------------------------------------------------------------------------
# Port für Web-Interface und REST-API
WEB_PORT=5000
